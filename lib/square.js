const Shapes = require('./shapes');

class Square extends Shapes {
    constructor(square, color) {
        super(square);
        this.color = color;
    }

    render() {
        return 
    }
}

module.exports = Square;
